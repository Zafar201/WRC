
// eslint-disable-next-line react/prop-types
function Header({name,option}) {
  return (
    <div>
        <h1>{name}</h1>
        <p> {option}</p>
        
       
    </div>
  )
}

export default Header